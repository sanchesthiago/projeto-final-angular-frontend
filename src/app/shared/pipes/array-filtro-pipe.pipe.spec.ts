import { ArrayFiltroPipePipe } from './array-filtro-pipe.pipe';

describe('ArrayFiltroPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayFiltroPipePipe();
    expect(pipe).toBeTruthy();
  });
});
